<% import static org.apache.commons.io.FilenameUtils.separatorsToUnix %>

{
    "builders": [
        {
            "vm_name": "${project.name}",
            "type": "virtualbox-ovf",
            "source_path": "${separatorsToUnix(project.baseBuildDir)}/${project.baseBox}",
            "output_directory": "${separatorsToUnix(project.boxBuildDir)}",
            "ssh_username": "box",
            "ssh_password": "box",
            "shutdown_command": "echo 'box' | sudo -S shutdown -P now",
            "headless": false,
            "vboxmanage": [
                ["modifyvm", "{{.Name}}", "--hwvirtex", "off"]
            ]
        }
    ],
    "provisioners": [
        {
            "type": "file",
            "source": "${project.ansibleBuildDir}",
            "destination": "/home/box"
        },
        {
            "type": "shell",
            "script": "${project.packerBuildDir}/provision.sh"
        }
    ],
    "post-processors": [
        {
            "type": "vagrant",
            "output": "${separatorsToUnix(project.vagrantBuildDir)}/${project.name}.box",
            "keep_input_artifact": true
        }
    ]
}

